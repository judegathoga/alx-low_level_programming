#include "main.h"

/**
 * add - Adds two integers and returns the result.
 * @one: First operand
 * @two: Second operand
 *
 * Return: The sum of one and two
 */
int add(int one, int two)
{
	return (one + two);
}
